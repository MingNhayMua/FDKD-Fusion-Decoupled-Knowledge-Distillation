# ══════════════════════════════════════════════════════
# FDKD Project — Requirements
# Extracted from OpenMMLab server environment (openmmlab conda env)
# ══════════════════════════════════════════════════════

# ── PyTorch (install via conda or pip+cu118/cu121) ──
torch==2.4.1
torchvision==0.20.0

# ── OpenMMLab Ecosystem (for training) ──
mmcv==2.1.0
mmengine==0.10.7
mmpretrain>=1.2.0
mmrazor>=1.0.0
openmim==0.3.9

# ── Model ──
timm==1.0.22

# ── Scientific ──
numpy>=1.24.0
scipy==1.10.1
Pillow>=9.5.0

# ── API Server ──
fastapi>=0.100.0
uvicorn>=0.23.0
python-multipart>=0.0.6

# ── Colab / Tunnel ──
pyngrok>=7.0.0
nest-asyncio>=1.5.0

# ── Visualization ──
pytorch-grad-cam>=1.5.0
matplotlib==3.7.5
seaborn==0.13.2

# ── Data / Utils ──
pandas==2.0.3
tqdm==4.65.2
tabulate==0.9.0
opencv-python==4.12.0.88
safetensors==0.5.3
huggingface-hub==0.36.0
transformers==4.46.3
einops==0.8.1
rich==13.4.2

# ── Misc ──
addict==2.4.0
yapf==0.43.0
PyYAML>=6.0
